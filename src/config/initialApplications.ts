import { Application } from '../types';

/**
 * Initial applications list.
 * Kept strictly empty to ensure NO demo or fake applications are present.
 */
export const INITIAL_APPLICATIONS: Application[] = [];
